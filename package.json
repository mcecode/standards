{
  "private": true,
  "scripts": {
    "format": "prettier . -w",
    "lint": "alex . && markdownlint-cli2-fix \"**/*.md\" \"#node_modules\" && prettier . -c"
  },
  "devDependencies": {
    "alex": "^10.0.0",
    "markdownlint-cli2": "^0.4.0",
    "prettier": "2.5.1"
  }
}
