{
  "private": true,
  "scripts": {
    "format": "prettier . -w",
    "lint": "alex . && markdownlint-cli2-fix \"**/*.md\" \"#node_modules\" && prettier . -c"
  },
  "devDependencies": {
    "alex": "^10.0.0",
    "markdownlint-cli2": "^0.3.2",
    "prettier": "2.4.1"
  }
}
