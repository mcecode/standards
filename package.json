{
  "private": true,
  "scripts": {
    "format": "prettier . -w",
    "lint": "alex . && markdownlint-cli2-fix \"**/*.md\" \"#node_modules\" && prettier . -c"
  },
  "devDependencies": {
    "alex": "^11.0.0",
    "markdownlint-cli2": "^0.6.0",
    "prettier": "2.8.3"
  }
}
